# Java 23 verification evidence — 2026-08-16

Environment:

- Windows / PowerShell
- `JAVA_HOME=D:\jdk mingw\jdk-23`
- Maven Wrapper from this repository

Reproduce:

```powershell
$env:JAVA_HOME='D:\jdk mingw\jdk-23'
.\mvnw.cmd clean verify
java -jar agent-cli\target\mini-claude-code.jar --version
java -jar agent-cli\target\mini-claude-code.jar --help
```

Observed result:

- Reactor: all six projects `SUCCESS`; total Maven time `02:39 min`.
- Unit tests: core 55, tools 157 (3 platform skips), RAG 70, integrations 67
  (1 platform skip), CLI 78; zero failures and zero errors.
- CLI integration tests: 3; zero failures and zero errors.
- Spotless: passed for every module.
- SpotBugs: zero findings for every module.
- JaCoCo line coverage:
  - `agent-core`: 55.36% (1574/2843)
  - `agent-tools`: 76.10% (1455/1912)
  - `agent-rag`: 89.31% (1979/2216)
  - `agent-integrations`: 80.44% (2237/2781)
  - `agent-cli`: 57.91% (2101/3628)
- The enforced 50% per-module line-coverage gate passed.
- Fat JAR: `agent-cli/target/mini-claude-code.jar` (94,166,279 bytes).
- Distribution ZIP and TAR.GZ were generated and non-empty.
- Fat-JAR `--version` and `--help` smoke tests exited 0; help lists only TUI startup and
  `config`, `index`, `rag` management commands, with no Agent `run` command.

Non-failing warnings seen during verification:

- Lucene recommends enabling the incubating Vector API for additional performance.
- Maven Shade reports duplicate license/notice/manifest resources, which is expected for the
  assembled dependency set.
- A fault-injection test deliberately logs simulated `disk full` audit failures.
